一，讲师申请驳回：
  客户端  1，点击驳回按钮弹出模太框，写提交表单
         2，点击提交后，调接口，将id，驳回理由msg发给后台，
  服务器  1，获得ajax请求接收的数据，操作数据库，将role：2，改为role：1
         2，建一张消息表格，将sendid,sendname,to,msg,sendtime写入数据库
         3，数据库写入成功，返回结果给前台
  客户端   1，根据后台结果，隐藏模太框后，成功重新ajax加载页面，失败弹出提示	